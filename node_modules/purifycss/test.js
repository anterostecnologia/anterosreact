var glob = require('glob');

// var files = {};
// glob.sync('**/*').forEach(function (pattern) {
//   glob.sync(pattern).forEach(function (pattern2) {
//     files[pattern2] = true;
//   });
// });

console.log(glob.sync('./node_modules/'));

// console.log(files);

var fs = require('fs');

// try {
//   fs.statSync(__dirname + '/hello/hello');
//   console.log('REACHED HERE');
// } catch (e) {
//   console.log('error');
// }
